import React from "react";
import { Button } from "react-bootstrap";

export default () => (
  <h1>
    <Button variant="primary" size="lg" block href="/list-users">
      Listar
    </Button>
  </h1>
);
